# Vercel installs THIS file when Root Directory = "/".
# Keep it lean (<250MB unzipped): NO torch/transformers/playwright.
# torch/transformers exceed Vercel's 250MB serverless limit.
# Gemini via direct REST (httpx), numpy optional pure-Python.
# For local scraping, use requirements-scraper.txt instead.
fastapi==0.110.2
uvicorn==0.29.0
pydantic==2.8.2
pydantic-settings==2.4.0
python-multipart==0.0.9
supabase>=2.15.0
openai==1.52.0
httpx==0.27.2
python-dotenv==1.0.1
mangum==0.17.0
